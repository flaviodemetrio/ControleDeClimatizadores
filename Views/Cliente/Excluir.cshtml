@model ClienteModel
@{
    ViewBag.Subtitulo = "Excluir Cliente";
}

<header class="d-flex justify-content-between align-items-center">
    <div>
        <h1 class="text-primary">@ViewBag.Subtitulo</h1>
    </div>
    
</header>
<hr>

<h4>Deseja realmente excluir o cliente <span class="text-danger">@Model.RazaoSocial</span></h4>


<form asp-action="Excluir" class="w-25">
    <input type="hidden" asp-for="@Model.IdCliente">
    <div class="mt-3">
        <a asp-action="Index" class="btn btn-success">Não</a>
        <button class="btn btn-danger" type="submit">Sim</button>
    </div>
</form>